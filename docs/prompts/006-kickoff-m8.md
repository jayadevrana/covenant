M6 and M7 gates are verified and committed (8 commits on main). The repo is now public at https://github.com/jayadevrana/covenant with remote `origin` configured. The reviewer ran the deferred Playwright command: after three strict-mode selector fixes (noted in EVIDENCE.md), the golden-path e2e passes 1/1. Vercel CLI is authenticated on this machine as `bluealgocapital-9965`.

Execute milestone M8 from docs/prompts/master-spec.md now, then stop with the final gate report and the full Definition of Done checklist with per-item evidence.

Operating notes:

1. Same rules: non-interactive, documented defaults, EVIDENCE.md rows, no git commits (print intended messages; the reviewer commits and pushes).
2. Deployment: use the authenticated Vercel CLI (`vercel deploy --prod --yes` after `vercel link --yes`). If the sandbox blocks CLI auth or network for the deploy step, prepare everything (vercel.json if needed, env instructions, exact commands) and print the exact command list for the reviewer to run — do not fail the milestone over sandbox limits. Set env var handling instructions for OPENAI_API_KEY (server-side only), MAX_LIVE_RUNS_PER_SESSION=3, DAILY_SPEND_KILL flag.
3. Spend controls per the spec: demo/fixture mode must work with NO key; live mode capped (max 3 live runs per session, existing max_output_tokens caps, kill-switch env honored). Implement whatever of this is missing.
4. Known deployment risk to handle honestly: the run store is in-memory via globalThis. On serverless, a cold start or second instance loses runs mid-flight. Mitigations to implement: (a) keep each run's full state client-refetchable (already true via runId → if a run disappears, the UI should show a graceful "sandbox session expired — start a fresh run" state, not a crash), and (b) label the sandbox as ephemeral in the UI footer. Do NOT add a database.
5. README completion per master-spec M8: setup from fresh clone, sample data note, testing instructions (unit + e2e + smoke scripts), Mermaid architecture diagram, Related Work paragraph (honest prior art: AWS AgentCore Policy, Invariant/Snyk, HumanLayer, Latitude, Trinitite, Progent paper — and precisely what Covenant does that each doesn't: the correction→eval→approval-gated policy patch loop with deterministic before/after replay, in a portable layer), "How GPT-5.6 is integrated" map (policy compiler, sandbox agent, correction compiler + terra classifier — with model IDs and where fixtures vs live apply), "How Codex built this project" narrative (this thread built all core functionality; describe the milestone workflow, where the human made key decisions per EVIDENCE.md, and where Codex accelerated the work), license/asset inventory (MIT, no third-party assets beyond npm OSS deps). Do not mention any AI tools other than Codex and OpenAI models anywhere.
6. Produce docs/JUDGE_CARD.md (demo URL placeholder to fill after deploy, 90-second test script matching the golden path, expected results, troubleshooting incl. "session expired" note), docs/DEMO_SCRIPT.md (165-second beat sheet per master-spec §10 demo spine, mapped to actual screens), docs/DEVPOST_DRAFT.md (title, tagline, description sections mapped explicitly to the four judging criteria, plus the built-with list and the "how Codex and GPT-5.6 were used" section).
7. Final audit: run the whole verification battery (unit, e2e if runnable, tsc, lint, build), the secret scan, and print the master-spec §10 Definition of Done checklist with evidence per item, marking anything that needs the reviewer (e.g., production URL smoke) as REVIEWER-ACTION with the exact command.

Begin now.
