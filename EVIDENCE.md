# Covenant build evidence

This ledger records milestone gates. M0–M2 commits were applied at review time. From M3 onward the execution sandbox mounts `.git` read-only, so each new row records the intended conventional commit for review-time application.

| Timestamp (Asia/Kolkata) | Task | Human decision | Codex contribution | Commit SHA | Test evidence | Demo-worthy moment |
|---|---|---|---|---|---|---|
| 2026-07-18 03:24 IST | M0 — Scaffold + hygiene | Use locked spec defaults; non-interactive run; no push because no remote | Generated Next.js 15 App Router + strict TypeScript + Tailwind scaffold; added MIT license, env hygiene, README provenance and integrity copy. Used a temporary npm cache after the machine cache returned EPERM; copied the completed scaffold after a post-generation create-next-app recursion error. Set local tracing root because a parent lockfile affected discovery. Browser screenshot deferred because the browser runtime reported no available browser. | `32f3cd1` | `npm run lint` exit 0; `npm run build` exit 0 (5/5 static pages, `/` prerendered); `WATCHPACK_POLLING=true npm run dev -- --port 3010` ready in 1.4s; `curl` returned `HTTP/1.1 200 OK` | Covenant landing skeleton boots locally without third-party branding |
| 2026-07-18 03:27 IST | M1 — Pure engine | Apply binding schema fields and documented defaults; trusted domains are deterministic derive context, with all 12 fixture customer domains trusted and the partner domain external | Added mirrored Zod/JSON schemas, pure condition and policy evaluation, deterministic derived facts, stable sorted-key canonicalization + SHA-256, 7-rule fixture policy, and golden trace. | `151ef53` | `npm test`: 1 file passed, 22 tests passed; `npx tsc --noEmit` exit 0; `npm run lint` exit 0 | Golden trace deterministically returns ALLOW, ALLOW, ALLOW, REQUIRE_APPROVAL, BLOCK |
| 2026-07-18 03:34 IST | M2 — OpenAI layer | Follow binding Responses API shapes; retry invalid output once; use `gpt-5.6` low effort and 2,000-token compiler cap. Key was present, so run both live sanity and seven-rule compiler checks. | Verified official rules and Structured Outputs pages with no contradiction; implemented policy/correction compilers, strict closed JSON schemas, mirrored Zod validation, graceful errors, raw invalid output, retry diagnostics, key-aware smoke scripts, and mocked client coverage. Initial smoke entrypoint used top-level await under CJS and was fixed before any API request. | `f7621d7` | `npm test`: 2 files passed, 28 tests passed. Live sanity: `gpt-5.6-sol`, 11 input + 5 output = 16 tokens. Live policy compiler: `gpt-5.6-sol`, 547 input + 1,094 output = 1,641 tokens, 7 valid rules. `npx tsc --noEmit` and `npm run lint` exit 0. | Plain-English seven-rule policy compiles live into a Zod-valid typed policy; invalid mocked output self-corrects on attempt two |
| 2026-07-18 12:55 IST | M3 — Sandbox agent + control room | Use in-memory per-run sandbox; client drives exactly one model turn per step; auto-approve only in the live gate harness; all UI approval remains explicit | Added six side-effect-free sandbox tools, one-turn agent core, function-call interception, structured block/deny feedback, approval and snapshot APIs, in-memory run store, and a dedicated fixture/live `/run` control room with provenance chips and approval modal. Browser screenshots remained unavailable because the browser inventory was empty; verified the production route and complete API walk over HTTP instead. | Intended `feat: add sandbox agent control room` | Final `npm test`: 5 files passed, 41 tests passed. Live `gpt-5.6-sol`: 6 turns; verdicts allow → allow → allow → require_approval → block; 4 sandbox executions, 1 outbox item, blocked partner execute=false. Fixture API walk repeated the sequence, recorded human approval, and reported `blocked_has_tool_execution=false`. Production `/run` returned HTTP 200. | Live agent proposes the leak, deterministic policy blocks it, and the execution recorder proves the tool never ran |
| 2026-07-18 12:55 IST | M4 — Receipts | Hash the canonical full event envelope excluding `payload_hash`; link the next event through the prior hash; do not describe receipts as signatures | Added receipt append/verify/export library, integrated eligible agent events, documented canonicalization, and added the dedicated `/receipts` chain UI with browser verification, head hash, readable payloads, provenance, and JSON export. | Intended `feat: add hash-linked action receipts` | Final `npm test`: 5 files passed, 41 tests passed. Targeted receipt suite: 3/3 passed; genuine export verifies green and a flipped payload byte verifies red with `Payload hash mismatch at 0`. Full fixture workflow emitted 25 linked events. Production `/receipts` returned HTTP 200. | The same run produces a readable chain that verifies green, while one changed byte proves red |
| 2026-07-18 12:55 IST | M5 — Immunity loop | Use `gpt-5.6-terra` as the authoritative cheap failure classifier, then one `gpt-5.6` correction compile; make the seed correction add narrow approval for the redacted summary so the generated case is red before and green after without weakening the leak block. The first live classifier response was empty, so the capped classifier was retried once; it succeeded before the single live correction compile ran. | Added exactly 12 seed evals, deterministic replay runner, correction/classification APIs, fixture and live compiler paths, patch preview/approve/reject, active per-run policy versions, correction/approval receipts, and dedicated `/immunity` UI with correction form, generated eval, patch diff, approval controls, before/after panels, and the full 13-case grid. | Intended `feat: add correction immunity loop` | Final `npm test`: 5 files passed, 41 tests passed; includes reject-no-change and subsequent runtime interception on approved policy v2. Successful live classifier `gpt-5.6-terra`: 402 tokens. Live compiler `gpt-5.6-sol`: 2,758 tokens. Proof: before 12/13, after 13/13, generated FAIL→PASS, regressions=0. Fixture HTTP workflow approved policy v2. Type-check, lint, build, and diff-check exit 0; production `/immunity` returned HTTP 200. | One human correction becomes a reviewable policy diff and permanent regression eval; activation produces a test-proven v2 with 13/13 green |
| 2026-07-18 13:18 IST | M6 — UX + reliability | Keep fixture mode as the keyless recovery path; surface the compiler's raw invalid output after its one retry; use port 3010 for isolated browser smoke; prefer an explicit focused approval control | Hardened contrast and focus-visible treatment, added reduced-motion support and approval-modal initial focus, preserved loading/empty/error states on every data screen, exposed raw compiler output in the visible error panel, and added a Playwright fixture smoke covering the landing, missing-key recovery, keyboard approval, all verdicts, receipt verify/export, invalid-output recovery, immunity proof, and v2 activation. | Intended `feat: harden demo UX and add Playwright golden path` | `npm test`: 6 files, 43 tests passed after M7; type-check/lint/build green. Playwright discovered and started the one golden-path test, but the managed macOS sandbox denied Chromium's Mach rendezvous (`Permission denied (1100)`); reviewer command: `npx playwright install chromium && npm run test:e2e`. Interactive browser inventory was also empty, so no screenshot could be captured here. | Demo mode remains usable after a missing-key banner, and malformed model output is inspectable rather than silent |
| 2026-07-18 13:18 IST | M7 — SDK + example + Integrate docs | Support Node 20+ and any framework with wrappable tool execution; default an absent approval hook to deny; keep HTTP transport requests camelCase while domain schemas remain binding snake_case | Added the in-repo `@covenant/sdk` package with `withCovenant(tools, policy, hooks)`, pure-engine interception, approval hook, structured refusals, receipt callbacks and verification; added SDK tests, a runnable minimal example, supported-platform/install/snippet docs, and all five API request shapes. | Intended `feat: add covenant SDK and minimal agent example` | SDK unit coverage proves blocked execute is never called and approved execution waits for the hook. `npx tsx examples/minimal-agent.ts`: `verdict=block`, `tool_executed=false`, `receipt_events=4`, `receipt_valid=true`. Final `npm test`: 6 files, 43 tests passed. | A framework-neutral wrapper blocks an external customer-data send and emits a valid receipt chain in one terminal command |
| 2026-07-18 15:38 IST | M8 — Ship | Use authenticated Vercel CLI; retain honestly labeled ephemeral in-memory sessions instead of adding a database; enforce at most three live starts per browser session; keep the spend kill switch off by default but authoritative when set to `1`; deploy the uncommitted review tree and leave commit/push to the reviewer | Added stateless HttpOnly live-run accounting, kill-switch and missing-key gates, request timeout recovery, consistent session-expired responses, ephemeral UI footer, deployment environment configuration, complete fresh-clone README, architecture/model/prior-art/Codex documentation, judge card, 165-second demo script, criteria-mapped Devpost draft, native favicon, release tests, and production deployment. Removed unused scaffold logo assets. | Intended `chore: ship covenant production release` | `npm test`: 9 files, 51 tests passed; type-check, lint, local build, Vercel build, diff check, SDK example, fresh-copy `npm ci`/test/build all green. Secret pattern scan: working tree 0, history 0; `.env.local` ignored; UI brand scan 0. Production HTTP routes all 200. Deployed fixture API proof: allow ×3 → require approval → block, blocked execution=false, receipts=23, before 12/13, after 13/13, regressions=0, policy v2. One deployed live `gpt-5.6-sol` run completed in 6 turns with the same verdict sequence, one sandbox outbox item, and blocked execution=false. Production fourth live start returned 429. Reviewer-verified pre-M8 Playwright: 1/1; this sandbox still denied Chromium launch after M8, so reviewer must rerun `PLAYWRIGHT_BASE_URL=https://covenant-umber.vercel.app npm run test:e2e` after applying the M8 commit. | Production at https://covenant-umber.vercel.app completes the full fixture governance and immunity loop over HTTP with no key |

> Reviewer note (2026-07-18 13:30 IST): three Playwright strict-mode selector fixes were applied at review time in e2e/golden-path.spec.ts (Next.js route-announcer collides with role=alert; duplicate verdict chips). App code unchanged. After the fixes the full golden-path e2e passes: 1 passed (15.4s).
